{
  "name": "node-envelope",
  "version": "1.0.9",
  "description": "A wrapper for the request module to allow for simpler RESTful API transactions.",
  "main": "src/envelope.js",
  "scripts": {
    "build": "gulp build",
    "lint": "gulp lint",
    "test": "mocha",
    "coverage": "istanbul cover node_modules/mocha/bin/_mocha --require test/**/*.js",
    "coveralls": "npm run coverage -- --report lcovonly && cat ./coverage/lcov.info | coveralls"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/nitro404/node-envelope.git"
  },
  "keywords": [
    "request",
    "envelope",
    "json",
    "rest",
    "api"
  ],
  "author": {
    "name": "Kevin Scroggins"
  },
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/nitro404/node-envelope/issues"
  },
  "homepage": "https://github.com/nitro404/node-envelope#readme",
  "dependencies": {
    "extra-utilities": "^1.3.4",
    "request": "^2.88.2"
  },
  "devDependencies": {
    "async": "^3.2.0",
    "body-parser": "^1.19.0",
    "chai": "^4.2.0",
    "express": "^4.17.1",
    "gulp-fabricator": "^1.1.0"
  }
}
